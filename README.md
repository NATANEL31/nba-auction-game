# מכרז 🏀

משחק מכרז שחקנים בזמן אמת. שחקנים מתחרים על חמישייה פותחת
בתקציב מוגבל, עם טיימר לכל תור.

- **client** — React + Vite, ממשק בעברית (RTL)
- **server** — Express + Socket.IO

---

## הרצה מקומית

```bash
npm run setup     # מתקין תלויות בקליינט ובשרת
npm run build     # בונה את client/dist
npm start         # מריץ על http://localhost:3001
```

או בפקודה אחת: `npm run serve`.

> `client/dist` **אינו** נשמר ב-git. חייבים `npm run build` לפני `npm start`,
> אחרת השרת יציג הודעה שמסבירה בדיוק מה להריץ.

### פיתוח

```bash
npm run dev:server   # שרת על 3001
npm run dev:client   # Vite על 5173, עם proxy ל-socket.io
```

---

## פריסה לענן

השרת מגיש גם את ה-API וגם את הקליינט, אז צריך שירות אחד בלבד.
הפרויקט כולל [`render.yaml`](render.yaml) מוכן ל-Render.

### Render (מומלץ, יש תוכנית חינם)

1. היכנסו ל-[render.com](https://render.com) והתחברו עם GitHub
2. **New → Blueprint**, בחרו את המאגר `nba-auction-game`
3. Render יקרא את `render.yaml` לבד. לחצו **Apply**
4. אחרי כ-3 דקות תקבלו כתובת כמו
   `https://nba-auction-game.onrender.com` — זו הכתובת שמחלקים לחברים

מה ש-Render מריץ:

| שלב | פקודה |
|---|---|
| Build | `npm run build` (מתקין הכל ובונה את הקליינט) |
| Start | `npm start` |
| Health check | `GET /healthz` |

### שני דברים שחשוב לדעת על התוכנית החינמית

**1. השירות נרדם.** אחרי ~15 דקות בלי תנועה Render מכבה אותו,
והכניסה הבאה לוקחת 30-60 שניות עד שהוא מתעורר. אחרי זה הכל מהיר.
שווה להזהיר את מי שמתחבר ראשון.

**2. הדיסק זמני.** `users.json` ו-`leaderboard.json` נמחקים בכל
הפעלה מחדש — כלומר **טבלת האלופים והסיסמאות מתאפסות** מדי פעם.

לפתרון בתוכנית בתשלום: בטלו את ההערה על בלוק ה-`disk` ב-`render.yaml`
והוסיפו `DATA_DIR=/var/data` ל-`envVars`. השרת יזהה את הדיסק לבד.
אם `DATA_DIR` לא ניתן לכתיבה, השרת לא קורס — הוא נופל חזרה
לאחסון זמני ורושם אזהרה בלוג.

### חלופות

Fly.io ו-Koyeb עובדים גם הם ותומכים ב-WebSockets. כל שירות שמריץ
Node ומכבד `PORT` יתאים — אין תלות ב-Render.

---

## משתני סביבה

| משתנה | ברירת מחדל | תפקיד |
|---|---|---|
| `PORT` | `3001` | פורט ההאזנה |
| `DATA_DIR` | תיקיית `server/` | היכן נשמרים טבלת האלופים והמשתמשים |

---

## חבילות שחקנים

| חבילה | שחקנים | מקור |
|---|---|---|
| NBA | 290 | ידני, בעברית |
| מכבי תל אביב | 111 | ידני, בעברית |
| יורוליג | 201 | מיובא מאקסל, שמות באנגלית |

**יורוליג:** העמדות מוינו לחמש קטגוריות (היברידיים לפי העמדה הראשונה,
`G` לפי גובה). האוברול חושב מהסטטיסטיקות — נקודות, ריבאונדים, אסיסטים,
חטיפות וחסימות חיובי, איבודים שלילי, אחוזי קליעה כתוספת — ומופה
להתפלגות נורמלית שתואמת את צורת מאגר ה-NBA (חציון 80, טווח 70-94).

178 שמות פוענחו במלואם מוויקיפדיה ואומתו מול האות הראשונה. 23 נשארו
מקוצרים (`E. Bryant`) במקום לנחש — התאמה שגויה הייתה מציגה שם **ותמונה**
של שחקן אחר.

## ערכות עיצוב ולוגו רקע

לכל חבילה ערכת צבעים משלה (`[data-theme]` ב-`index.css`) ולוגו ענק ברקע.
הלוגו נטען כ-CSS mask ולכן **נצבע אוטומטית** בצבע ההדגשה של הערכה.

להחלפת לוגו: הפילו קובץ ל-`client/public/logos/` בשם `nba.svg`,
`maccabi.svg` או `euroleague.svg`. **רקע שקוף וצורה נקייה** — הצבע
המקורי לא משנה. הקבצים הנוכחיים הם סילואטות גנריות, לא הלוגואים
הרשמיים (סימני מסחר).

## תמונות שחקנים

456 מתוך 602 השחקנים מגיעים עם תצלום מוויקיפדיה העברית
(NBA 70%, מכבי 81%, יורוליג 80%). מי שאין לו תמונה מקבל עיגול
ראשי תיבות בצבע קבוע שנגזר משמו.

- הקבצים: `client/public/players/pNNN.jpg` (240px, ~33KB בממוצע)
- המיפוי: `server/playerPhotos.json` — שם שחקן ← נתיב תמונה
- השרת מצרף `photo` לכל שחקן, והלקוח נופל לאווטאר אם חסר

**להוסיף תמונה חסרה:** שימו קובץ ב-`client/public/players/`
(שם באנגלית בלבד, למשל `p295.jpg`) והוסיפו שורה ל-`playerPhotos.json`.
שמות קבצים בעברית או עם גרש **לא** עובדים — לכן המיפוי קיים.

התמונות מוויקיפדיה ברישיון חופשי ברובן.

## מוזיקה

ראו [`client/public/audio/README.md`](client/public/audio/README.md).

---

## חוקי המשחק

- כל שחקן מתחיל עם **$20** וחמש עמדות: PG, SG, SF, PF, C
- **15 שניות** לכל תור
- מי שפותח מכרז על שחקן **חייב להציע לפחות $0** — אי אפשר לפרוש
  בהצעה הראשונה. אם הזמן נגמר, מוגשת עבורו הצעה של $0
- אחרי שהוגשה הצעה, אפשר לפרוש בכל תור
- אפשר להתחיל משחק **גם לבד**, לבדיקות
- בסוף המשחק מסדרים את החמישייה ומכתירים זוכה, שמקבל נקודה בטבלה
