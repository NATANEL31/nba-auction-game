/**
 * שכבת הרקע עם לוגו הליגה בענק.
 * הצורה נצבעת דרך CSS mask, כך שהיא תמיד בגוון הערכה הפעילה.
 */
export default function Background() {
  return <div className="app-bg" aria-hidden="true" />;
}
